# secrets — never commit (GitLab token, Slack bot token)
config.json

# personal local runbook (contains company-specific details)
本機使用說明.md
deploy/local/

# runtime state / locks / logs
state.json
state.tmp
.lock
reviews/
*.log
*.log.*

# script <-> AI engine handoff artifacts
mr_context.json
final_findings.json

# python
__pycache__/
*.pyc
*.tmp
